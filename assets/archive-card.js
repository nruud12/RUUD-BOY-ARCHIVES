/* ==========================================================
   DEPRECATED — intentionally empty.

   This file previously contained a single orphaned statement
   (`card.classList.add("is-playing");`) with no surrounding scope,
   which would throw a ReferenceError if it were ever loaded.

   Card playback + active-state logic now lives in archive-card-ui.js.
   Kept as an empty stub so any stale asset_url reference resolves
   harmlessly rather than 404ing.
========================================================== */
